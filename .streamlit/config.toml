[theme]
base = "light"
primaryColor = "#2563eb"
backgroundColor = "#f8fafc"
secondaryBackgroundColor = "#eef2ff"
textColor = "#0f172a"
font = "sans serif"

[server]
headless = true

# Removes the default Streamlit strip above the app (deploy / extra chrome).
[ui]
hideTopBar = true
